<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MonkeysMVVM.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MonkeysMVVM.Views"
    Shell.FlyoutBehavior="Flyout">

    <!--<TabBar Shell.TabBarUnselectedColor="CornflowerBlue">
        <ShellContent
            Title="Find Monkey By Location"
            ContentTemplate="{DataTemplate local:FindMonkeyByLocationPage}"
            Icon="home.png"
            Route="FindByLocation" />
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MonkeysPage}"
            Icon="search.png"
            Route="MainPage" />
    </TabBar>-->
    <FlyoutItem Title="Home" FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent
            Title="Find Monkey By Location"
            ContentTemplate="{DataTemplate local:FindMonkeyByLocationPage}"
            Icon="Monkey.png"
            Route="FindByLocation" />
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MonkeysPage}"
            Icon="Home.jpg"
            Route="MainPage" />

    </FlyoutItem>
</Shell>
